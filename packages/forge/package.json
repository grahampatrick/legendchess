{
  "name": "@playthelegend/forge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Authoring pipeline: PGN + curation config in, validated puzzle JSON with Stockfish eval tables out.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "cli": "tsx src/cli.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@playthelegend/core": "workspace:*",
    "chessops": "^0.15.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "tsx": "^4.22.4"
  }
}
